# 이 프로젝트는 환경변수가 필요하지 않습니다.
# Vercel / Netlify 배포 시 별도 설정 없이 바로 배포 가능합니다.
#
# 향후 외부 API (예: 실제 MCP 서버 연결)를 추가한다면
# 아래 형식으로 환경변수를 추가하세요:
#
# NEXT_PUBLIC_SOME_KEY=your_value_here   ← 브라우저에서 접근 가능
# SECRET_KEY=your_secret_here            ← 서버에서만 접근 가능
